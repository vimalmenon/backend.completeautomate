class OneTimeScript:

    def start(self) -> bool:
        # Need to add when there is some transform data
        return False

    def transform(self) -> bool:
        # Need to add when there is some transform data
        return False
